extends layout
block content
    body
        script.
          window.googleClientId = "#{googleClientId}"
          window.expressEnv = "#{expressEnv}"
          window.isAuthenticated = #{isAuthenticated}
        span(class="view-container")