mutation createSecret($input: CreateSecretInput!) {
    createSecret(input: $input) {
        secretId
        description
        createdTimestamp
        lastUsedTimestamp
        secretToken
    }
}
