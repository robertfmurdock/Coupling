div(class="tribe-config" ng-class="self.styles.className" ng-show="self.tribe")
    div
        h1 Tribe Configuration
    div
        span
            ul(ng-class="self.styles.editor")
                li
                    label(for="tribe-name") Name
                    input(id="tribe-name" type="text" ng-model="self.tribe.name" placeholder="Enter the tribe name here")
                    span The full tribe name!
                li
                    label(for="tribe-email") Email
                    input(id="tribe-email" type="text" ng-model="self.tribe.email" placeholder="Enter the tribe email here")
                    span The tribe email address - Attach a Gravatar to this to choose your tribe icon.
                li(ng-show="self.isNew")
                    label(for="tribe-id") Unique Id
                    input(id="tribe-id" type="text" ng-model="self.tribe.id")
                li
                    label Enable Call Signs
                    input(id="call-sign-checkbox" type="checkbox" ng-model="self.tribe.callSignsEnabled")
                    span Every Couple needs a Call Sign. Makes things more fun!
                li
                    label Enable Badges
                    input(id="badge-checkbox" type="checkbox" ng-model="self.tribe.badgesEnabled")
                    span Advanced users only: this lets you divide your tribe into two groups.
                li
                    label Default Badge Name
                    input(id="default-badge-name" type="text" ng-model="self.tribe.defaultBadgeName")
                    span The first badge a player can be given. When badges are enabled, existing players default to having this badge.
                li
                    label Alt Badge Name
                    input(id="alt-badge-name" type="text" ng-model="self.tribe.alternateBadgeName")
                    span The other badge a player can be given. A player can only have one badge at a time.
                li
                    label Pairing Rule
                    select(
                    id="pairing-rule"
                    ng-model="self.tribe.pairingRule"
                    ng-options="rule.id as rule.description for rule in self.pairingRules"
                    )
                    span Advanced users only: This rule affects how pairs are assigned.
        tribecard(tribe="self.tribe" size=200)
    div
        input(id="save-tribe-button"
        type="button"
        class="super blue button save-button"
        ng-click="self.clickSaveButton()"
        tabIndex="0"
        value="Save")
        div(ng-hide="self.isNew" class="small red button delete-tribe-button" ng-click="self.removeTribe()") Retire