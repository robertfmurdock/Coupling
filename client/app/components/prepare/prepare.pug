div(ng-class="prepare.styles.className")
    div
        tribebrowser(tribe="prepare.tribe")
    div
        div
            a(
            ng-class="prepare.styles.spinButton"
            class="spin-button super pink button"
            ng-click="prepare.clickSpinButton()"
            ) Spin!
        playercard(ng-repeat="selectable in prepare.selectablePlayers"
        player="selectable.player"
        tribe-id="selectable.tribe.id"
        ng-class="{disabled:!selectable.isSelected}"
        ng-click="prepare.clickPlayerCard(selectable)"
        disabled="true"
        )