dependencies:
  override:
    - npm rebuild
    - node node_modules/protractor/bin/webdriver-manager update --standalone
test:
  override:
    - npm test
deployment:
  staging:
    branch: master
    heroku:
      appname: armored-coupling
