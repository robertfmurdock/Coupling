dependencies:
  override:
    - npm rebuild
    - node node_modules/protractor/bin/webdriver-manager update --standalone
test:
    - npm test
    - npm run protractor
deployment:
  staging:
    branch: master
    heroku:
      appname: armored-coupling
