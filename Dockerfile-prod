FROM node:7-alpine

WORKDIR /usr/src/app

ENV \
  PUBLIC_HOST=web \
  MONGOHQ_URL=mongodb://mongo/Coupling \
  MONGO_CONNECTION=mongodb://mongo \
  NODE_ENV=production

COPY . /usr/src/app

RUN npm install --production

CMD ["npm", "start"]
EXPOSE 3000 8125
