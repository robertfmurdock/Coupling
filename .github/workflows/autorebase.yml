name: Autorebase
on:
  push:
    branches-ignore:
      - rebase-pull-request**
      - cherry-pick-rebase-pull-request**
  pull_request:
    types: [ labeled ]

jobs:
  auto-rebase:
    name: AutoRebase
    runs-on: ubuntu-latest
    steps:
      - name: Auto rebase
        uses: MoveLab-Studio/AutoRebase@v0.2.0
        with:
          github_token: ${{ secrets.COUPLING_PAT }}
