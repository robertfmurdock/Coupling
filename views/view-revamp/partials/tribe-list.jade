div(class="tribe-listing-frame")
    div(ng-show="tribes" class="tribe-listing")
        span(ng-repeat='tribe in tribes' class="tribe" ng-click="clickOnTribeCard(tribe)")
            div(class="tribe-name" ng-click="clickOnTribeName(tribe)") {{tribe.name}}
            img(ng-src="{{tribe | tribeImageUrl: {size: 150} }}" width="150" height="150")
    div
        a(type="button" id="new-tribe-button" class="super green button" href="/new-tribe") Add a new tribe!