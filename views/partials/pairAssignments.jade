div
    h4 {{formattedDate}}
    div(ng-repeat='pair in pairAssignmentDocument.pairs') Pair!
        ul
            li(ng-repeat='player in pair') Name:  {{player.name}}

    input(type="button" value="Save!" ng-click="save()" ng-hide="pairAssignmentDocument._id")