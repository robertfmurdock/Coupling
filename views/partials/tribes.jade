div(class="tribe-listing-frame")
    div(class="tribe-listing")
        span(ng-repeat='tribe in tribes' class="tribe" ng-click="selectTribe(tribe)")
            div(class="tribe-name") {{tribe.name}}
            img(ng-src="{{tribe | tribeImageUrl}}" width="150" height="150")